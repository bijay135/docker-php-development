version: '2'

services:
  webserver:
    build: ./docker/webserver
    image: sakwo.sastodeal.com:4567/bijay135/php-ci-cd
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./www:/var/www/html
    links:
      - db

  db:
    image: mysql:5.7
    ports: 
      - "3306:3306"
    volumes:
      - ./db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=php-test
